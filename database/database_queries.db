CREATE TABLE Saves (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(30),
    description BLOB,
    profile INTEGER,
    date DATE,
    time TIME
);

INSERT INTO Saves (name, description, profile, date, time)
VALUES ("Test save", "Save for testing", 1, date('now'), time('now'));